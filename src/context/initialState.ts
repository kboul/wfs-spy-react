import { versions, requests } from '../shared/constants';

export default {
    url: '',
    version: versions[0],
    request: requests[0],
    wfsRequest: '',
    wfsResponse: '',
    parsedGetCapResp: null
};
