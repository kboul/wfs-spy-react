import { CSSProperties } from "react";

const id = "selectedTypValueRef";

const label = "Selected typeName\n & valueReference";

const labelStyle: CSSProperties = {
  paddingTop: "20px",
  whiteSpace: "pre-wrap"
};

export { id, label, labelStyle };
