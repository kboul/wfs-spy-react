const versions = ['2.0.0', '2.0.2'];

const requests = ['GetCapabilities', 'DescribeFeatureType', 'GetPropertyValue'];

const noOperation = 'This WFS service does not provide ';

const proccessMessage = 'processing request...';

const noOption = '---';

export { versions, requests, noOperation, proccessMessage, noOption };
